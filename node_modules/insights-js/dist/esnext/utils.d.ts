export declare function isInBrowser(): boolean;
/**
 * Get the current host, including the protocol, origin and port (if any).
 *
 * Does **not** end with a trailing "/".
 */
export declare function getHost(): string;
export declare function isReferrerSameHost(): boolean;
export declare function isInIframe(): boolean;
